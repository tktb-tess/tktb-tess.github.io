---
import HomeIcon from '../assets/HomeIcon.svg';

interface Props {
  readonly url: string;
}

interface MdProps {
  readonly url: string;
  readonly frontmatter: {
    readonly title: string;
    readonly description?: string;
  };
}

const mds = Object.values(
  import.meta.glob<MdProps>('../pages/**/*.md', { eager: true }),
).map((md) => ({ url: md.url, title: md.frontmatter.title }) as const);

const getTitle = (href: string) => {
  if (href !== '/') {
    return mds.find((m) => m.url === href)?.title ?? '[NO DATA]';
  } else {
    return;
  }
};

const { url } = Astro.props;
const dirs = url.split('/').filter((s) => s !== '');

const pages = dirs.map((_, i) => {
  const h_ = dirs.slice(0, i).join('/');
  const href = h_ === '' ? '/' : `/${h_}/`;
  const title = getTitle(href);
  return { href, title } as const;
});

const currentText = getTitle(url);
---

<nav class='breadcrumb' aria-label='パンくずリスト'>
  <ol>
    {
      pages.map(({ href, title }) => {
        return (
          <li>
            <a {href} title={title ?? 'Home'}>
              {title ?? <HomeIcon class='size-4' />}
            </a>
          </li>
        );
      })
    }
    <li>
      <a href='#' aria-current='page' tabindex='-1'>{currentText}</a>
    </li>
  </ol>
</nav>

<style lang="postcss">
  @reference '../styles/global.css';
  @layer components {
    .breadcrumb {
      @apply my-4 block;
    }

    ol {
      @apply list-none flex flex-wrap gap-2 ps-0 m-0;
    }

    li {
      @apply contents;
    }

    a,
    li:not(:first-child)::before {
      @apply flex-[unset] w-max min-w-0 leading-none;
    }

    a[aria-current="page"] {
      @apply font-semibold pointer-events-none ctext-text;
    }

    li:not(:first-child)::before {
      content: '>';
    }
  }
</style>
